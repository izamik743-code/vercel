# 🚀 Быстрый старт с локальной базой данных

## ⚡ За 5 минут

### 1. Установите зависимости
```bash
npm install
```

### 2. Настройте базу данных
```bash
# Создайте базу и таблицы
npm run db:push

# Добавьте тестовые данные
npm run db:init
```

### 3. Запустите проект
```bash
npm run dev
```

### 4. Откройте в браузере
```
http://localhost:3000
```

## 🎯 Что получите

- ✅ **Локальная база данных** - SQLite файл `prisma/dev.db`
- ✅ **Авторизация работает** - пользователи создаются в базе
- ✅ **Баланс отображается** - реальные данные из базы
- ✅ **Кейсы работают** - предметы сохраняются в инвентарь
- ✅ **Админ панель** - управление пользователями
- ✅ **Prisma Studio** - веб-интерфейс для базы данных

## 🔧 Полезные команды

```bash
# Открыть Prisma Studio (веб-интерфейс для базы)
npm run db:studio

# Сбросить базу данных и добавить тестовые данные
npm run db:reset

# Только сгенерировать Prisma клиент
npm run db:generate

# Только применить изменения схемы
npm run db:push
```

## 📊 Просмотр данных

### Через Prisma Studio
```bash
npm run db:studio
```
Откроется веб-интерфейс на `http://localhost:5555`

### Через SQLite Browser
1. Скачайте [SQLite Browser](https://sqlitebrowser.org/)
2. Откройте файл `prisma/dev.db`
3. Просматривайте таблицы `users`, `transactions`, `inventory`

## 🐛 Если что-то не работает

### Ошибка "Prisma client not found"
```bash
npm run db:generate
```

### Ошибка "Database not found"
```bash
npm run db:push
```

### Ошибка "User not found"
```bash
npm run db:init
```

### Полный сброс
```bash
npm run db:reset
```

## 🎮 Тестирование

### 1. Создание пользователя
- Откройте приложение
- Пользователь автоматически создастся в базе
- Проверьте в Prisma Studio

### 2. Подключение кошелька
- Нажмите на кнопку подключения кошелька
- Введите тестовый адрес: `UQAjU_dKuBVzeAQOfqNZ5kqUGsuPBXY9bjW1Cs4ZT_eTANGy`
- Проверьте обновление в базе

### 3. Открытие кейсов
- Нажмите на кейс
- Предмет добавится в инвентарь
- Баланс уменьшится

### 4. Админ панель
- Перейдите на `/admin`
- Введите пароль: `admin123`
- Управляйте пользователями

## 📁 Файлы базы данных

- `prisma/dev.db` - основная база данных SQLite
- `prisma/schema.prisma` - схема базы данных
- `lib/prisma.ts` - Prisma клиент

## 🔄 Миграция с Supabase

Если у вас уже есть данные в Supabase:

1. **Экспортируйте данные** из Supabase
2. **Импортируйте в SQLite** через Prisma Studio
3. **Обновите код** - замените Supabase на Prisma
4. **Протестируйте** все функции

## 🚀 Для продакшена

### Вариант 1: PostgreSQL на Vercel
```bash
# Установите PostgreSQL
npm install pg @types/pg

# Обновите schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

### Вариант 2: PlanetScale (MySQL)
```bash
# Установите MySQL клиент
npm install @planetscale/database

# Обновите schema.prisma
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
```

## 🎉 Готово!

Теперь у вас есть полностью рабочее приложение с локальной базой данных. Никаких внешних зависимостей, все работает локально!

### Следующие шаги:
1. **Протестируйте все функции**
2. **Добавьте больше тестовых данных**
3. **Настройте для продакшена** (если нужно)
4. **Деплойте на Vercel**

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в консоли браузера
2. Запустите `npm run db:studio` для просмотра данных
3. Выполните `npm run db:reset` для полного сброса
4. Обратитесь за помощью с логами ошибок
